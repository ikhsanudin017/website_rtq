{
  "buildCommand": "npm install && npx prisma generate && npm run build",
  "framework": "nextjs",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["sin1"]
}
